# Serving dependencies - inference only (no training libraries)

# Core ML inference (CPU-only slim build)
torch==2.2.2
torchvision==0.17.2

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Image processing
Pillow==10.3.0

# Utilities
numpy==1.26.4
